apiVersion: v1
kind: Service
metadata:
  name: hello
  namespace: default
spec:
  selector:
    app: hello
  ports:
  - name: http
    port: 5000
    protocol: TCP
    targetPort: 5000
  type: NodePort
